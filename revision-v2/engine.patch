diff --git a/Makefile b/Makefile
index 83278a9..023723e 100644
--- a/Makefile
+++ b/Makefile
@@ -4,7 +4,7 @@ all:
 	$(PYTHON) tools/build.py
 assets:
 	$(PYTHON) tools/build_content.py
-	$(PYTHON) tools/generate_graphics.py
+	$(PYTHON) tools/graphics_v2.py
 	$(PYTHON) tools/build_audio.py
 check:
 	$(PYTHON) tests/validate_rom.py
 diff --git a/lorom.cfg b/lorom.cfg
index 7c33acb..17ffc24 100644
--- a/lorom.cfg
+++ b/lorom.cfg
@@ -46,6 +46,10 @@ SEGMENTS {
  OBJ: load=ROM2,type=ro;
  AUDIO: load=ROM11,type=ro;
  MAPS: load=ROM12,type=ro;
+ HEROGFX: load=ROM13,type=ro;
+ FAR0: load=ROM14,type=ro;
+ FAR1: load=ROM15,type=ro;
+ FARMAPS: load=ROM16,type=ro;
  CHR0: load=ROM3,type=ro;
  CHR1: load=ROM4,type=ro;
  CHR2: load=ROM5,type=ro;
 diff --git a/src/main.s b/src/main.s
index b25ac59..def1356 100644
--- a/src/main.s
+++ b/src/main.s
@@ -95,6 +95,16 @@ soundarg: .res 2
 soundready: .res 2
 prevy: .res 2
 respawn: .res 2
+vx: .res 2
+xfrac: .res 2
+yfrac: .res 2
+jumpbuffer: .res 2
+coyote: .res 2
+animtick: .res 2
+heropose: .res 2
+heroloaded: .res 2
+heropiece: .res 2
+herorows: .res 2
 
 .segment "STATE"
 state_begin:
@@ -175,15 +185,20 @@ Reset:
  sta $2105
  lda #$60
  sta $2107
- stz $2108
+ lda #$64
+ sta $2108
  stz $2109
  stz $210a
- stz $210b
+ lda #$70
+ sta $210b
  stz $210c
  stz $210d
  stz $210d
- stz $210e
- stz $210e
+ lda #$ff
+ sta $210e
+ sta $210e
+ sta $2110
+ sta $2110
  stz $211b
  stz $211b
  lda #$80
@@ -196,7 +211,7 @@ Reset:
  stz $2130
  stz $2131
  stz $2133
- lda #$11
+ lda #$13
  sta $212c
  stz $212d
  lda #$e0
@@ -205,6 +220,7 @@ Reset:
  .a16
  jsr AudioInit
  jsr LoadObjects
+ jsr ResetMotion
  lda #$ffff
  sta oldregion
  jsr CheckSave
@@ -340,6 +356,24 @@ UploadFrame:
  stz $4304
  lda #1
  sta $420b
+ rep #$20
+ .a16
+ jsr UploadHero
+ lda px
+ lsr
+ lsr
+ lsr
+ sep #$20
+ .a8
+ sta $210f
+ stz $210f
+ lda mode
+ cmp #2
+ bcc :+
+ lda #$11
+ bra :++
+: lda #$13
+: sta $212c
  rep #$20
  .a16
  rts
@@ -433,9 +467,11 @@ LoadRegion:
  asl
  asl
  clc
+ asl
+ asl
  adc #.loword(RegionPal)
  sta $4302
- lda #32
+ lda #128
  sta $4305
  sep #$20
  .a8
@@ -451,6 +487,7 @@ LoadRegion:
  .a16
  lda region
  sta oldregion
+ jsr LoadFarBackground
  rts
 BaseMap:
  lda region
@@ -756,6 +793,7 @@ Recalculate:
 @done:rts
 EnterRoom:
  jsr Blank
+ jsr ResetMotion
  lda #0
  sta form
  sta vy
@@ -885,7 +923,7 @@ RoomMap:
 @hud: sta MAP,x
  inx
  inx
- cpx #256
+ cpx #192
  jne @hud
  ldx #(25*64)
 @foot: sta MAP,x
@@ -909,7 +947,7 @@ RoomMap:
  jmp @platform
 @platformA: ldx #(17*64+6*2)
 @platform:
- lda #97
+ lda #186
  ldy #7
 @pl: sta MAP,x
  inx
@@ -923,6 +961,22 @@ RoomMap:
  inx
  dey
  jne @pl2
+ lda #185
+ ldx room
+ txa
+ and #1
+ jeq :+
+ ldx #(17*64+8*2)
+ jmp :++
+: ldx #(17*64+6*2)
+: lda #185
+ sta MAP,x
+ lda #187
+ sta MAP+12,x
+ lda #185
+ sta MAP+(13*64+21*2)
+ lda #187
+ sta MAP+(13*64+27*2)
  ; Doors are ground-floor alcoves at x24,88,152,216.
  lda #0
  sta exitidx
@@ -953,10 +1007,10 @@ RoomMap:
  lda secrets,x
  and BitMasks,y
  jne @visible
- lda #105
+ lda #158
  jmp @tile
 @visible:
- lda #108
+ jsr DoorArtTile
 @tile:
  sta t0
  lda exitidx
@@ -965,18 +1019,11 @@ RoomMap:
  asl
  asl
  clc
- adc #(20*64+3*2)
+ adc #(18*64+2*2)
  tax
- lda t0
- ora #$2000
- sta MAP+64,x
- sta MAP+128,x
- sta MAP+66,x
- sta MAP+130,x
- lda #$2066
- sta MAP,x
- lda #$2067
- sta MAP+2,x
+ lda #5
+ sta t3
+ jsr DrawFixture
 @next:
  inc exitidx
  lda exitidx
@@ -986,15 +1033,16 @@ RoomMap:
  lda f:room_type,x
  and #1
  jeq @notshrine
- lda #$206e
- sta MAP+(21*64+14*2)
- sta MAP+(22*64+14*2)
- lda #$206d
- sta MAP+(22*64+15*2)
+ lda #173
+ sta t0
+ lda #4
+ sta t3
+ ldx #(19*64+13*2)
+ jsr DrawFixture
 @notshrine:
  lda #$2000
  sta textcolor
- lda #(3*64+2)
+ lda #(2*64+2)
  sta textpos
  lda room
  jsr NameRoom
@@ -1122,202 +1170,6 @@ TickTimers:
  jcs @done
  inc hp
 @done:rts
-MovePlayer:
- lda py
- sta prevy
- lda #2
- sta t0
- lda form
- cmp #1
- jne @speed
- lda #3
- sta t0
-@speed:
- lda accessory
- cmp #28
- jne @armorspeed
- lda frame
- and #3
- jne @armorspeed
- inc t0
-@armorspeed:
- lda armor
- cmp #13
- jne @dash
- lda frame
- and #7
- jne @dash
- inc t0
-@dash:
- lda pressed
- and #JOY_L
- jeq @moving
- lda dashcd
- jne @moving
- lda #12
- sta dash
- lda #45
- sta dashcd
- lda #18
- sta inv
-@moving:
- lda dash
- jeq @directions
- lda #5
- sta t0
- lda face
- jeq @right
- jmp @left
-@directions:
- lda pad
- and #JOY_LEFT
- jne @left
- lda pad
- and #JOY_RIGHT
- jne @right
- jmp @jump
-@left:
- lda #1
- sta face
- lda px
- sec
- sbc t0
- cmp #8
- jcs :+
- lda #8
-: sta px
- jmp @jump
-@right:
- stz face
- lda px
- clc
- adc t0
- cmp #232
- jcc :+
- lda #232
-: sta px
- jmp @jump
-@jump:
- lda form
- cmp #2
- jcs @fly
- lda pressed
- and #JOY_B
- jeq @gravity
- lda #1
- sta t0
- lda inventory+62
- jeq :+
- inc t0
-: lda jumps
- cmp t0
- jcs @gravity
- inc jumps
- lda #$fff8
- sta vy
- lda #2
- jsr SFX
-@gravity:
- lda frame
- and #3
- jne @fall
- lda vy
- cmp #7
- jeq @fall
- inc vy
-@fall:
- lda py
- clc
- adc vy
- sta py
- cmp #40
- jcs @floor
- lda #40
- sta py
- stz vy
- jmp @floor
-@fly:
- lda pad
- and #(JOY_B|JOY_UP)
- jeq @flydown
- lda py
- sec
- sbc #2
- cmp #40
- jcs :+
- lda #40
-: sta py
- jmp @floor
-@flydown:
- lda py
- inc
- sta py
- lda pad
- and #JOY_DOWN
- jeq @floor
- inc py
-@floor:
- lda py
- cmp #152
- jcc @platforms
- lda #152
- sta py
- stz vy
- stz jumps
-@platforms:
- lda form
- cmp #2
- jcs @done
- lda vy
- jmi @done
- lda prevy
- cmp #104
- jcc @lower
- jeq @lower
- jmp @upper
-@lower:
- lda py
- cmp #104
- jcc @upper
- lda room
- and #1
- jeq :+
- lda #64
- jmp :++
-: lda #48
-: sta t0
- lda px
- clc
- adc #12
- cmp t0
- jcc @upper
- lda t0
- clc
- adc #56
- cmp px
- jcc @upper
- lda #104
- sta py
- stz vy
- stz jumps
-@upper:
- lda prevy
- cmp #72
- jcc :+
- jeq :+
- jmp @done
-: lda py
- cmp #72
- jcc @done
- lda px
- cmp #224
- jcs @done
- cmp #156
- jcc @done
- lda #72
- sta py
- stz vy
- stz jumps
-@done:rts
 CycleWeapon:
  ldx #12
 @next:
@@ -1373,6 +1225,7 @@ CycleForm:
  jsr SFX
  rts
 
+.include "polish.s"
 .include "combat.s"
 .include "render.s"
 .include "menu_save.s"
 diff --git a/src/render.s b/src/render.s
index 6ea439c..45349b0 100644
--- a/src/render.s
+++ b/src/render.s
@@ -9,79 +9,48 @@ DrawGame:
  lda #$30
  sta sattr
  stz ssize
- lda inv
- jeq @hero
- lda frame
- and #4
- jne @enemies
-@hero:
+ lda form
+ jne @animal
+ jsr DrawHuman
+ jmp @enemies
+@animal:
  lda px
+ sec
+ sbc #8
  sta sx
  lda py
- sta sy
- lda form
- jeq @human
- cmp #1
- jeq @wolf
- cmp #2
- jeq @bat
- lda #128
- jmp @animal
-@wolf:lda #64
- jmp @animal
-@bat:lda #72
-@animal:
- sta stile
- lda frame
- and #8
- jeq :+
- lda stile
- clc
- adc #4
- sta stile
-: lda #1
- sta ssize
- lda sy
  clc
  adc #16
  sta sy
- lda face
- jeq :+
- lda #$70
- sta sattr
-: jsr AddSprite
- stz ssize
- jmp @enemies
-@human:
+ lda form
+ dec
+ asl
+ asl
+ sta t0
  lda frame
  lsr
  lsr
  lsr
- and #3
+ and #1
  asl
+ clc
+ adc t0
+ tax
+ lda f:AnimalTiles,x
  sta stile
- lda pad
- and #(JOY_LEFT|JOY_RIGHT)
+ lda form
+ cmp #3
  jne :+
- stz stile
-: lda attack
- jeq :+
- lda #8
- sta stile
-: lda face
+ lda py
+ sta sy
+: lda #1
+ sta ssize
+ lda face
  jeq :+
  lda #$70
  sta sattr
 : jsr AddSprite
- lda stile
- clc
- adc #32
- sta stile
- lda sy
- clc
- adc #16
- sta sy
- jsr AddSprite
+ stz ssize
 @enemies:
  lda #$30
  sta sattr
@@ -96,15 +65,41 @@ DrawGame:
  lda enemyy,x
  sta sy
  lda enemykind,x
+ cmp #1
+ jne @groundEnemy
+ lda frame
+ and #8
+ jeq :+
+ lda #68
+ jmp :++
+: lda #12
+: sta stile
+ lda #1
+ sta ssize
+ lda sx
+ sec
+ sbc #8
+ sta sx
+ lda sy
+ clc
+ adc #8
+ sta sy
+ jsr AddSprite
+ stz ssize
+ jmp @next
+@groundEnemy:
  asl
  tax
  lda f:EnemyTiles,x
  sta stile
- jsr AddSprite
- ldx loopidx
- lda enemykind,x
- cmp #1
- beq @next
+ lda frame
+ and #8
+ jeq :+
+ lda stile
+ sec
+ sbc #64
+ sta stile
+: jsr AddSprite
  lda stile
  clc
  adc #32
@@ -292,75 +287,86 @@ DrawGame:
  jsr DrawHUD
  rts
 DrawHUD:
- lda #$2000
+ lda #$2c00
  sta textcolor
- TEXT HpLabel,0,1
- lda #6
+ lda #$2c72
+ sta MAP+2
+ lda #4
  sta textpos
  lda hp
- jsr PrintNumber
- TEXT MpLabel,0,9
- lda #22
+ jsr PrintNumber3
+ lda #$2c73
+ sta MAP+32
+ lda #34
  sta textpos
  lda mp
- jsr PrintNumber
- TEXT LvLabel,0,17
- lda #38
+ jsr PrintNumber3
+ lda #$2c2c
+ sta MAP+56
+ lda #58
  sta textpos
  lda level
- jsr PrintNumber
- TEXT GoldLabel,0,25
- lda #54
- sta textpos
- lda gold
- jsr PrintNumber
- lda #66
- sta textpos
- lda weapon
- jsr NameItem
- ; Full-width bars: twenty-four health cells on row2.
+ jsr PrintNumber2
+ ; Eight health cells, seven ether cells, no full-width number wall.
  lda hp
+ asl
+ asl
+ asl
  sta t0
  lda maxhp
- lsr
- lsr
- lsr
- lsr
  sta t1
- lda #0
- sta t2
- ldx #(2*64+2)
-@bar:
+ ldx #14
+ ldy #8
+@hpbar:
  lda t0
  cmp t1
- jcc @empty
+ jcc @hpempty
  sec
  sbc t1
  sta t0
- lda #$2076
- jmp @put
-@empty:
- lda #$2077
-@put:
+ lda #$2c76
+ jmp @hpcell
+@hpempty:
+ lda #$2c77
+@hpcell:
  sta MAP,x
  inx
  inx
- inc t2
- lda t2
- cmp #16
- jcc @bar
- lda form
- asl
+ dey
+ jne @hpbar
+ lda mp
  asl
+ sta t0
  asl
  clc
- adc #.loword(FormNames)
- sta ptr
- lda #.bankbyte(FormNames)
- sta ptr+2
- lda #(2*64+42)
+ adc t0
+ sta t0
+ lda maxmp
+ sta t1
+ ldx #42
+ ldy #6
+@mpbar:
+ lda t0
+ cmp t1
+ jcc @mpempty
+ sec
+ sbc t1
+ sta t0
+ lda #$2cc8
+ jmp @mpcell
+@mpempty:
+ lda #$2cc9
+@mpcell:
+ sta MAP,x
+ inx
+ inx
+ dey
+ jne @mpbar
+ ; The equipped sword remains named on its own line.
+ lda #66
  sta textpos
- jsr Print
+ lda weapon
+ jsr NameItem
  ldx #(25*64)
  lda #0
 @clear:
@@ -507,7 +513,7 @@ IRQ:
 BitMasks: .word 1,2,4,8
 Decimal: .word 1000,100,10,1
 RegionCHRBank: .word 3,4,5,6,7,8,9,10
-EnemyTiles: .word 192,72,196,200
+EnemyTiles: .word 192,12,196,200
 BossTiles: .word 256,260,264,268,320,324,328,332,384,388
 OrbitX: .word 32,30,24,12,0,65524,65512,65506,65504,65506,65512,65524,0,12,24,30
 OrbitY: .word 12,24,36,42,44,42,36,24,12,0,65526,65520,65518,65520,65526,0
 diff --git a/src/rom_data.s b/src/rom_data.s
index f7a932b..8ae0bce 100644
--- a/src/rom_data.s
+++ b/src/rom_data.s
@@ -38,6 +38,28 @@ RegionPal:
 .incbin "assets/bg_region5.pal"
 .incbin "assets/bg_region6.pal"
 .incbin "assets/bg_region7.pal"
+.segment "HEROGFX"
+HeroFrames: .incbin "assets/hero-frames.chr"
+.segment "FAR0"
+.incbin "assets/far_region0.chr"
+.incbin "assets/far_region1.chr"
+.incbin "assets/far_region2.chr"
+.incbin "assets/far_region3.chr"
+.segment "FAR1"
+.incbin "assets/far_region4.chr"
+.incbin "assets/far_region5.chr"
+.incbin "assets/far_region6.chr"
+.incbin "assets/far_region7.chr"
+.segment "FARMAPS"
+FarMaps:
+.incbin "assets/far_region0.map"
+.incbin "assets/far_region1.map"
+.incbin "assets/far_region2.map"
+.incbin "assets/far_region3.map"
+.incbin "assets/far_region4.map"
+.incbin "assets/far_region5.map"
+.incbin "assets/far_region6.map"
+.incbin "assets/far_region7.map"
 .segment "HEADER"
 .byte "SINFONIA CASTLEVANICA"
 .byte $20,$02,$0A,$03,$01,$33,$00
